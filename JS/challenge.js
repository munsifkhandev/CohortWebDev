// Challenge 1 :

function totalCupcakes(trayOne, trayTwo) {
  return trayOne + trayTwo;
}
console.log("Total number of Cupckaes Per Tray :", totalCupcakes(12, 20));

// Challenge 2 :

function totalChocolates(barPerBox, numberOfBoxes) {
  return barPerBox * numberOfBoxes;
}
console.log("Total number of Chocolates Per box :", totalChocolates(9, 4));

// Challenge 3 :

function myName(name) {
  console.log(`My name is ${name}`);
}
myName("Munsif Khan");

// Challenge 4 :

function totalStrength(Boys, Girls) {
  return Boys - Girls;
}
console.log(totalStrength(38, 12));

function myClass(larky, larkiyan) {
  let totalStrength = 50;

  console.log(`Total Strength is ${totalStrength}`);
  console.log("Larkiyon ki strength :", totalStrength - larky);
}

myClass(38);
